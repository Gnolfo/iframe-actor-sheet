{{#if hasUrl}}
<iframe class="iframe-actor-sheet__frame" src="{{iframeUrl}}" allow="clipboard-read; clipboard-write"></iframe>
{{else}}
<div class="iframe-actor-sheet__empty">Configure the iframe URL in module settings.</div>
{{/if}}
